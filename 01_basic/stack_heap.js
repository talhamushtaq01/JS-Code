
//stack and heap are two memory allocation where date is Store

//stack for primitive datatype and heap for non-primitive datatype.


let number = "email@gmail.om";
let anothernumber = number;
anothernumber = "email"
console.log(anothernumber);
console.log(number)

// if declare the one variable value and we call first variable value in second variable so it copy of first variable, so we change value of second variable the value of first variable will same.  
//in stack we take reference it give copy of it.

//heap
//In heap we take referene it give original not give copy.

let objects